<div class="signees">

    <figure>
        <img src="/img/chicken-mcnugget.jpg" alt="fried chicken head">
    </figure>
    <h2>We, the undersigned, demand that real chicken be used to make our chicken nuggets!</h2>

    <ul>
        {{#each results}}
        <li>{{#if this.homepage}}
            <a href="{{this.homepage}}">{{this.first_name}} {{this.last_name}} </a>
            {{else}}
                {{this.first_name}} {{this.last_name}}
            {{/if}}
            {{#if this.age}}
                 ({{this.age}}{{#if this.city}}, <a href="/petition/signatures/{{this.city}}">{{this.city}}</a>{{/if}})
            {{else if this.city}}
                (<a href="/petition/signatures/{{this.city}}">{{this.city}}</a>)
            {{/if}}
        </li>
        {{/each}}
    </li>
</div>
